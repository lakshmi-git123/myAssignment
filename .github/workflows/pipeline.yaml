name: CI/CD Pipeline
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@main
        with:
          version: 'latest'
          project_id: ${{ secrets.myassignment-426912 }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}

      - name: Configure Docker to use gcloud as a credential helper
        run: |
          gcloud auth configure-docker

      - name: Build Docker image
        run: |
          docker build -t gcr.io/${{ secrets.myassignment-426912}}/myassignment:$GITHUB_SHA

      - name: Push Docker image
        run: |
          docker push gcr.io/${{ secrets.myassignment}}/myassignment:$GITHUB_SHA

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy DI-PY-api \
            --image gcr.io/${{ secrets.myassignment}}/myassignment:$GITHUB_SHA \
            --platform managed \
            --region ${{ secrets.GCP_REGION }} \
            --allow-unauthenticated

